# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.ruff_cache/

# Environments
.venv/

# Generated artifacts. Everything the pipeline writes lives under the configured
# ${root} (conf/config.yaml); these root-anchored entries cover the default,
# which keeps it inside the checkout. Anchored so src/conv_wm/data/ and
# tests/data/ are never excluded.
/data/
/reports/
/outputs/

# Notebooks
.ipynb_checkpoints/

# Editors and OS
.obsidian/
.idea/
.vscode/
.DS_Store
