# Label and cue ontology

Planned capability; not part of the current pipeline. Annotation sources carry
their provenance and semantics (see `05_annotation_audit.md`) so that labels can
be derived later with explicit provenance.
