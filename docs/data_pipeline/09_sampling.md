# Temporal representation and model-ready packaging

Planned capability; not part of the current pipeline. Must honour the temporal
contract of `03_temporal_audit.md`: native PTS as the clock, no naive PCM
concatenation for long Ego4D audio.
